## CLI Report file for "@tomaskral-tests/rhdh-cli"

> Do not edit this file. It is a report generated by `yarn build:api-reports`

### `rhdh-cli`

```
Usage: rhdh-cli [options] [command]

Options:
  -V, --version
  -h, --help

Commands:
  plugin [command]
  help [command]
```

### `rhdh-cli plugin`

```
Usage: rhdh-cli plugin [options] [command] [command]

Options:
  -h, --help

Commands:
  export [options]
  package [options]
  help [command]
```

### `rhdh-cli plugin export`

```
Usage: rhdh-cli plugin export [options]

Options:
  --minify
  --embed-package [package-name...]
  --shared-package [package-name...]
  --override-interop <mode:package-name,package-name...>
  --allow-native-package [package-name...]
  --suppress-native-package [package-name...]
  --ignore-version-check [packageName...]
  --no-install
  --no-build
  --clean
  --dev
  --dynamic-plugins-root <dynamic-plugins-root>
  --in-place
  --scalprum-config <file>
  --track-dynamic-manifest-and-lock-file
  -h, --help
```

### `rhdh-cli plugin package`

```
Usage: rhdh-cli plugin package [options]

Options:
  --force-export
  --preserve-temp-dir
  --export-to <directory>
  -t, --tag <tag>
  --use-docker
  --platform <platform>
  -h, --help
```
